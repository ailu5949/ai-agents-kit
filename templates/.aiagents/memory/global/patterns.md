# 成功模式 (Patterns)

> 跨项目可复用的"做对了"经验。Claude 每轮任务开始前先扫一眼这里。

格式约定：每条用 `## YYYY-MM-DD HH:MM` 开头，下面一行简介，再列 1~3 条要点。

例：

## 2026-04-28 15:30

- Spring Boot 接口分页统一用 `Page<T>` 返回，前端可直接复用既有列表组件
- 模式适用：所有 list 类 GET 接口（不含搜索过滤复杂查询）

---
